__pycache__/
*.pyc
.env
.claude/
refernce/
*.tsbuildinfo

# Word lock files
~$*

# node deps (frontend build deps, doc tooling) - never committed;
# the deployed app is served from the prebuilt bundle in static/
node_modules/
